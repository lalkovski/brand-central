const ROOT = "/";
const BRANDS = "/brands";
const BRAND_ADD = "/brands/add";
const BRAND_EDIT = "/brands/:id";

const ROUTES = {
  ROOT,
  BRANDS,
  BRAND_ADD,
  BRAND_EDIT,
};

export default ROUTES;
