import Flex from 'components/Flex'
import styled from 'styled-components'

export const Text = styled.p`
  margin: 0;
  user-select: none;
`

export const SortWrapper = styled(Flex)`
  cursor: pointer;
`
